import { QuerybinderDirective } from './querybinder.directive';

describe('QuerybinderDirective', () => {
  it('should create an instance', () => {
    const directive = new QuerybinderDirective();
    expect(directive).toBeTruthy();
  });
});
